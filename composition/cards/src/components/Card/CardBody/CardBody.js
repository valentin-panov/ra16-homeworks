/* eslint-disable no-unused-vars */
import React from 'react';
import PropTypes from 'prop-types';

function CardBody(props) {
  return <div className='Card_body'>{props.children}</div>;
}

CardBody.propTypes = {};

export default CardBody;
